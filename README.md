# Data Structures and Algorithms (DSA) — C Language

This repository contains my **Data Structures and Algorithms** work in **C**, including notes, activities, and implementations.
Folders are organized by topic for easy navigation and testing.

---

## 📂 Folder Structure

- `01-array-list/` — Array List (static / dynamic operations)
- `02-linked-list/` — Linked List (basic operations)
- `03-array-stack/` — Stack using Array
- `04-array-queue/` — Queue using Array
- `05-linked-list-stack/` — Stack using Linked List
- `06-linked-list-queue/` — Queue using Linked List
- `07-cursor-based-list/` — Cursor-Based (Virtual Heap) List
- `08-bit-vector-set/` — Bit Vector / Bitmask Set (multiple variations)

- `09-hashing/` — Hashing
  - Open Hashing (Separate Chaining)
  - Closed Hashing (Open Addressing: Linear Probing / Quadratic Probing / Double Hashing)
  - Internal vs External Hashing (notes + examples)

- `10-priority-queue/` — Priority Queue (heap-based)
- `11-heap-sort/` — Heap Sort
- `12-binary-tree/` — Binary Tree / BST
  - Operations: insert, delete, search
  - DFS Traversals: inorder, preorder, postorder
  - BFS Traversal: level-order

---

## 🛠️ How to Compile and Run (GCC)

Open your terminal inside the folder of the program you want to run:

```bash
gcc filename.c -o run
./run
